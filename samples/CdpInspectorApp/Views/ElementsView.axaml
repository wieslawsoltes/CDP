<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="CdpInspectorApp.Views.ElementsView"
             x:CompileBindings="False">
    <Grid ColumnDefinitions="*, 4, 1.2*">
        <!-- DOM Tree (Left Side) -->
        <Grid RowDefinitions="Auto, *, Auto" Margin="10">
            <!-- Search -->
            <Grid ColumnDefinitions="*, Auto" Margin="0,0,0,8">
                <TextBox Name="txtSearch" Text="{Binding Elements.SearchQuery, Mode=TwoWay}" Watermark="Search by tag, ID, class, or text content..." Height="28" VerticalContentAlignment="Center"/>
                <Button Name="btnSearch" Command="{Binding Elements.SearchCommand}" Grid.Column="1" Content="Search" Margin="5,0,0,0" Padding="10,0" Height="28"/>
            </Grid>

            <!-- treeDom -->
            <Border Grid.Row="1" Classes="panel-card" ClipToBounds="True">
                <TreeView Name="treeDom" ItemsSource="{Binding Elements.RootNodes}" SelectedItem="{Binding Elements.SelectedNode, Mode=TwoWay}">
                    <TreeView.ItemTemplate>
                        <TreeDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <TextBlock Text="{Binding DisplayName}" Foreground="{Binding ForegroundBrush}" FontSize="12"/>
                            </StackPanel>
                        </TreeDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Border>

            <!-- Actions Footer -->
            <Grid Grid.Row="2" ColumnDefinitions="Auto, *, Auto" Margin="0,8,0,0">
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                    <TextBlock Text="Inspected:" Foreground="#9aa0a6" FontSize="11"/>
                    <TextBlock Name="txtSelectedNodeId" Text="{Binding Elements.SelectedNodeIdText}" FontWeight="Bold" Foreground="#8ab4f8" FontSize="11"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                    <Button Name="btnFocus" Command="{Binding Elements.FocusSelectedNodeCommand}" Content="Scroll Into View" Height="28" Padding="10,0"/>
                    <CheckBox Name="chkHighlight" IsChecked="{Binding Elements.IsHighlightActive, Mode=TwoWay}" IsEnabled="{Binding Connection.IsConnected}" Content="Inspect Highlight" FontSize="11" Margin="5,0" VerticalAlignment="Center"/>
                    <Button Name="btnDeleteControl" Command="{Binding Elements.DeleteSelectedNodeCommand}" Content="Delete Element" Height="28" Padding="10,0" Background="#b71c1c" Foreground="White" BorderBrush="#b71c1c"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Background="#3c4043"/>

        <!-- Styles & Attributes sidebar (Right Side) -->
        <Grid Grid.Column="2" Margin="10">
            <TabControl Padding="0,5,0,0">
                <!-- Styles subtab -->
                <TabItem Header="Styles">
                    <TabControl Padding="0,5,0,0" Margin="0,5,0,0">
                        <TabItem Header="Inline styles">
                            <Grid RowDefinitions="*, Auto" Margin="0,5,0,0">
                                <ListBox Name="listCssProperties" ItemsSource="{Binding Elements.CssProperties}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid ColumnDefinitions="*, Auto">
                                                <StackPanel Orientation="Horizontal" Spacing="5">
                                                    <TextBlock Text="{Binding Name}" Foreground="#f28b82" FontWeight="SemiBold" FontSize="12"/>
                                                    <TextBlock Text=":" Foreground="#9aa0a6" FontSize="12"/>
                                                    <TextBlock Text="{Binding Value}" Foreground="#e8eaed" FontSize="12"/>
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <Grid Grid.Row="1" ColumnDefinitions="*, Auto" Margin="0,8,0,0">
                                    <TextBox Name="txtStyleText" Text="{Binding Elements.StyleTextInputText, Mode=TwoWay}" Watermark="Add style rule (e.g. background: red)..." Height="28" VerticalContentAlignment="Center"/>
                                    <Button Name="btnApplyStyleText" Command="{Binding Elements.ApplyStyleTextCommand}" Grid.Column="1" Content="Apply" Margin="5,0,0,0" Padding="10,0" Height="28"/>
                                </Grid>
                            </Grid>
                        </TabItem>
                        <TabItem Header="Computed rules">
                            <ListBox Name="listComputedStyles" ItemsSource="{Binding Elements.ComputedStyles}" Margin="0,5,0,0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="*, Auto">
                                            <StackPanel Orientation="Horizontal" Spacing="5">
                                                <TextBlock Text="{Binding Name}" Foreground="#81c995" FontWeight="SemiBold" FontSize="12"/>
                                                <TextBlock Text=":" Foreground="#9aa0a6" FontSize="12"/>
                                                <TextBlock Text="{Binding Value}" Foreground="#e8eaed" FontSize="12"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </TabItem>
                    </TabControl>
                </TabItem>

                <!-- Attributes subtab -->
                <TabItem Header="Attributes">
                    <Grid RowDefinitions="*, Auto" Margin="0,5,0,0">
                        <ListBox Name="listAttributes" ItemsSource="{Binding Elements.Attributes}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnDefinitions="*, Auto">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock Text="{Binding Name}" Foreground="#fdd663" FontWeight="SemiBold" FontSize="12"/>
                                            <TextBlock Text="=" Foreground="#9aa0a6" FontSize="12"/>
                                            <TextBlock Text="{Binding Value}" Foreground="#81c995" FontSize="12"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Grid Grid.Row="1" ColumnDefinitions="*, *, Auto, Auto" Margin="0,8,0,0">
                            <TextBox Name="txtAttrName" Text="{Binding Elements.AttributeNameInputText, Mode=TwoWay}" Watermark="Name" Height="28" VerticalContentAlignment="Center" Margin="0,0,4,0"/>
                            <TextBox Grid.Column="1" Name="txtAttrValue" Text="{Binding Elements.AttributeValueInputText, Mode=TwoWay}" Watermark="Value" Height="28" VerticalContentAlignment="Center" Margin="0,0,4,0"/>
                            <Button Grid.Column="2" Name="btnApplyAttr" Command="{Binding Elements.ApplyAttributeCommand}" Content="Apply" Padding="10,0" Height="28" Margin="0,0,4,0"/>
                            <Button Grid.Column="3" Name="btnDeleteAttr" Command="{Binding Elements.DeleteAttributeCommand}" Content="Delete" Padding="10,0" Height="28"/>
                        </Grid>
                    </Grid>
                </TabItem>

                <!-- Properties subtab -->
                <TabItem Header="Properties">
                    <Grid RowDefinitions="*, Auto" Margin="0,5,0,0">
                        <ListBox Name="listProperties" ItemsSource="{Binding Elements.Properties}" SelectedItem="{Binding Elements.SelectedProperty, Mode=TwoWay}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnDefinitions="*, Auto">
                                        <StackPanel Spacing="2">
                                            <StackPanel Orientation="Horizontal" Spacing="5">
                                                <TextBlock Text="{Binding Name}" Foreground="#8ab4f8" FontWeight="SemiBold" FontSize="12"/>
                                                <TextBlock Text=":" Foreground="#9aa0a6" FontSize="12"/>
                                                <TextBlock Text="{Binding Value}" Foreground="#e8eaed" TextWrapping="Wrap" FontSize="12"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Type, StringFormat='({0})'}" Foreground="#9aa0a6" FontSize="10"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Grid Grid.Row="1" ColumnDefinitions="Auto, Auto, *, Auto" Margin="0,8,0,0">
                            <TextBlock Text="Selected:" VerticalAlignment="Center" Foreground="#9aa0a6" Margin="0,0,5,0" FontSize="11"/>
                            <TextBlock Name="lblSelectedProperty" Text="{Binding Elements.SelectedPropertyNameText}" FontWeight="Bold" Foreground="#8ab4f8" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="11"/>
                            <TextBox Grid.Column="2" Name="txtPropertyValue" Text="{Binding Elements.PropertyValueInputText, Mode=TwoWay}" Watermark="Value..." Height="28" VerticalContentAlignment="Center"/>
                            <Button Grid.Column="3" Name="btnApplyProperty" Command="{Binding Elements.ApplyPropertyCommand}" Content="Set" Margin="5,0,0,0" Padding="10,0" Height="28"/>
                        </Grid>
                    </Grid>
                </TabItem>

                <!-- Event Listeners subtab -->
                <TabItem Header="Event Listeners">
                    <ListBox Name="listEventListeners" ItemsSource="{Binding Elements.EventListeners}" Margin="0,5,0,0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="*, Auto">
                                    <StackPanel Spacing="2" Margin="2">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock Text="{Binding Type}" Foreground="#fdd663" FontWeight="SemiBold" FontSize="12"/>
                                            <TextBlock Text="➔" Foreground="#9aa0a6" FontSize="12"/>
                                            <TextBlock Text="{Binding HandlerName}" Foreground="#81c995" FontSize="12"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding CaptureText, StringFormat='(Capture: {0})'}" Foreground="#9aa0a6" FontSize="10"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
